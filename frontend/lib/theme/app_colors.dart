import 'package:flutter/material.dart';

class AppColors {
  // Earthen Sanctuary Palette
  // Core brand identity / High-priority actions
  static const Color primary = Color(0xFF526352); // Sage
  // Large hero surfaces and vibrant accents
  static const Color primaryContainer = Color(0xFF899B88); // Lighter Sage
  // Emotional touchpoints and wellness tracking
  static const Color secondary = Color(0xFF8A4F32); // Terracotta
  // Emotional touchpoints (Fixed/Light version)
  static const Color secondaryFixed = Color(0xFFFFDBCC); // Light Peach
  static const Color onSecondaryFixed = Color(0xFF351000); // Dark Brown
  
  // Success states and health "vital" indicators
  static const Color tertiary = Color(0xFF5D5F5E); // Charcoal/Stone
  
  // Background / Canvas
  static const Color surface = Color(0xFFFBF9F6); // Sand
  // Tonal Layering Levels
  static const Color surfaceContainerLow = Color(0xFFF5F3F0);
  static const Color surfaceContainer = Color(0xFFEFEEEB);
  static const Color surfaceContainerHigh = Color(0xFFEAE8E5);
  static const Color surfaceContainerHighest = Color(0xFFE4E2DF);
  static const Color surfaceContainerLowest = Color(0xFFFFFFFF);
  
  static const Color onSurface = Color(0xFF1B1C1A); // Near Black
  static const Color onSurfaceVariant = Color(0xFF434843); // Greyscale variants
  
  static const Color primaryFixed = Color(0xFFD4E8D3); // Pale green for focus
  static const Color onPrimaryFixed = Color(0xFF101F12);
}
